# Baseline Hotspots

This file is generated from benchmark index data.

Run:

```bash
cargo run --bin perf-hotspots -- --from target/criterion
```

and paste/update the ranked output table here for the current baseline snapshot.
